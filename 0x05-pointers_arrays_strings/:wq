#include "main.h"
#include <string.h>

/**
 * puts_half - prints half of a string
 * @str: string
 *
 * return: void
 */

void puts_half(char *str)
{
	int i, len = strlen(str);

	for (i = ((len - 1) / 2) + 1; i < len; i++)
	{
		putchar(*(str + 1));
	}
	putchar(*(str + 1));
}
