#include "main.h"
#include <stdlib.h>
/**
 *abs - check the absolute value;
* @int - parameter
*
* Description: check for absolute values
*
* Return: returns value;
*
*/

int _abs(int f)
{
	int g;

	g = abs(f);
	return (g);
}
