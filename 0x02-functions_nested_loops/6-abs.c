#include "main.h"
#include <stdlib.h>
/**
*_abs - check the absolute value;
* @f - parameter
*
* Description: check for absolute values
*
* Return: returns value;
*
*/

int _abs(int f)
{
	int g;

	g = abs(f);
	return (g);
}
