#include <stdio.h>

/**
 * main - emtry point
 *
 * for - runs a loop to iterate the alphabet
 *
 * Return: returns 0 aftre nd of the loop
 *
 */

int main (void)
{
	int bet;
	    bet = 'a';

	for (bet <= 'z'; bet != 'e'; bet != 'q';)
	{
		putchar(bet);
		bet++;
	};
	putchar('\n');

	Return (0);
}
