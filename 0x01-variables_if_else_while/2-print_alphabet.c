#include <stdio.h>

/**
 * main - entry point
 *
 * Return: returns 0 if value is true;
 *
 */

int main(void)
{
	int y;

	for (y = 'a'; y <= 'z'; y++)
	{
		putchar(y);
	};
	putchar ('\n')
	return (0);
}
