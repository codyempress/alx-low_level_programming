#include <stdio.h>
#include "main.h"

/**
 * mul - mutiplies two digits
 * @a: digit one
 * @b: digit two
 *
 * Return: returns 0
 */

int mul(int a, int b)
{
	int prod;

	prod = a * b;
	return (prod);
}
